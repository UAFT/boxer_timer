V22 fixes:
- round-start cue restored on every round start (initial and after rest)
- rest-start cue restored on every work->rest transition
- warning countdown rebuilt as per-second tick sequence instead of long 3/5/10 files
- pause/reset stop currently playing warning/transition audio immediately
- storage key bumped to v6
